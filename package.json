{
  "name": "part3",
  "version": "1.0.0",
  "engines": {
    "node": "15.12.0",
    "npm": "7.10"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../part2/part2-2 && npm run build --prod && cp -r build uniHelsinki/part3",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "proxy": "https://localhost:3001",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "morgan-body": "^2.6.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
